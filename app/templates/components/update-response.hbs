{{#if updateResponseForm}}
<h1>Update Response</h1>
  <div>
    <form>
      <div class="form-group">
        <label for="answer">Answer</label>
        {{input value=answer id="answer"}}
      </div>
      <div class="form-group">
        <label for="writer">Your Name</label>
        {{input value=writer id="writer"}}
      </div>
      <button {{action 'update' response}}>Save</button>
    </form>
  </div>
{{else}}
  <button {{action 'updateResponseForm'}}>Update</button>
{{/if}}
